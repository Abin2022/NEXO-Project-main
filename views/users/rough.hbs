
<script>
         function validateForm() {
             var name = document.getElementById("name").value;
             var email = document.getElementById("email").value;
             var phone = document.getElementById("phone").value;
             var password = document.getElementById("password").value;
             var confirmPassword = document.getElementById("confirm_password").value;

             // Regular expressions
             var nameRegex = /^[a-zA-Z\s]+$/;
             var emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
             var phoneRegex = /^(\+91|0)?[6789]\d{9}$/;
             var passwordRegex = /^(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}$/;

             if (!nameRegex.test(name)) {
                 alert("Name is not valid");
                 return false;
             }

             if (!emailRegex.test(email)) {
                 alert("Email is not valid");
                 return false;
             }

             if (!phoneRegex.test(phone)) {
                 alert("Phone number is not valid");
                 return false;
             }

             if (!passwordRegex.test(password)) {
                 alert("Password is not valid. It should contain at least 8 characters, one uppercase letter, one lowercase letter, and one digit.");
                 return false;
             }

             if (confirmPassword !== password) {
                 alert("Confirm Password should match the Password");
                 return false;
             }

             // Perform other custom validation checks
             // ...

             return true;
         }
     </script>





















     <form method="post" action="/signup" onsubmit="return validateForm()">
  <div class="input-group">
    <i class='bx bxs-user'></i>
    <input type="text" name="name" placeholder="Username" id="name" required>
  </div>
  <div class="input-group">
    <i class='bx bx-mail-send'></i>
    <input type="email" name="email" placeholder="Email" id="email" required>
  </div>
  <div class="input-group">
    <i class='bx bxs-lock-alt'></i>
    <input class="message" type="number" name="mobile" placeholder="Mobile" id="phone" required>
  </div>
  <div class="input-group">
    <i class='bx bxs-lock-alt'></i>
    <input type="password" name="password" placeholder="Password" id="password" required>
  </div>
  <div class="input-group">
    <i class='bx bxs-lock-alt'></i>
    <input type="password" name="confirm_password" placeholder="Confirm Password" id="confirm_password" required>
  </div>
  <button>Sign up</button>
  <p>
    <div style="text-align: center;">
      <span>Already have an account?</span>
      <a href="/login">Sign in here</a>
      <br>
    </div>
  </p>
</form>

<script>
  function validateForm() {
    var name = document.getElementById("name").value;
    var email = document.getElementById("email").value;
    var phone = document.getElementById("phone").value;
    var password = document.getElementById("password").value;
    var confirmPassword = document.getElementById("confirm_password").value;

    // Regular expressions
    var nameRegex = /^[a-zA-Z\s]+$/;
    var emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    var phoneRegex = /^(\+91|0)?[6789]\d{9}$/;
    var passwordRegex = /^(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}$/;

    if (!nameRegex.test(name)) {
      alert("Name is not valid");
      return false;
    }

    if (!emailRegex.test(email)) {
      alert("Email is not valid");
      return false;
    }

    if (!phoneRegex.test(phone)) {
      alert("Phone number is not valid");
      return false;
    }

    if (!passwordRegex.test(password)) {
      alert("Password is not valid. It should contain at least 8 characters, one uppercase letter, one lowercase letter, and one digit.");
      return false;
    }

    if (confirmPassword !== password) {
      alert("Confirm Password should match the Password");
      return false;
    }

    // Perform other custom validation checks
    // ...

    return true;
  }
</script>
























<!DOCTYPE html>
<html>
<head>
  <!-- Styles and other head content... -->
</head>
<body>
  <form method="post" action="/signup" onsubmit="return validateForm()">
    <!-- Form content... -->
     <h1>Sign Up</h1>
    <div class="input-group">
      <i class="fas fa-user"></i>
      <input type="text" name="name" placeholder="Username" id="name" required>
    </div>
    <div class="input-group">
      <i class="fas fa-envelope"></i>
      <input type="email" name="email" placeholder="Email" id="email" required>
    </div>
    <div class="input-group">
      <i class="fas fa-phone"></i>
      <input class="message" type="number" name="mobile" placeholder="Mobile" id="phone" required>
    </div>
    <div class="input-group">
      <i class="fas fa-lock"></i>
      <input type="password" name="password" placeholder="Password" id="password" required>
    </div>
    <div class="input-group">
      <i class="fas fa-lock"></i>
      <input type="password" name="confirm_password" placeholder="Confirm Password" id="confirm_password" required>
    </div>
    <button>Sign up</button>
    <p>
      <span>Already have an account?</span>
      <a href="/login">Sign in here</a>
    </p>
    <button type="submit">Sign up</button>
    <p>
      <span>Already have an account?</span>
      <a href="/login">Sign in here</a>
    </p>
  </form>

  <script>
    function validateForm() {
      // Validate form fields...

       var name = document.getElementById("name").value;
    var email = document.getElementById("email").value;
    var phone = document.getElementById("phone").value;
    var password = document.getElementById("password").value;
    var confirmPassword = document.getElementById("confirm_password").value;

    // Regular expressions
    var nameRegex = /^[a-zA-Z\s]+$/;
    var emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    var phoneRegex = /^(\+91|0)?[6789]\d{9}$/;
    var passwordRegex = /^(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}$/;

    if (!nameRegex.test(name)) {
      alert("Name is not valid");
      return false;
    }

    if (!emailRegex.test(email)) {
      alert("Email is not valid");
      return false;
    }

    if (!phoneRegex.test(phone)) {
      alert("Phone number is not valid");
      return false;
    }

    if (!passwordRegex.test(password)) {
      alert("Password is not valid. It should contain at least 8 characters, one uppercase letter, one lowercase letter, and one digit.");
      return false;
    }

    if (confirmPassword !== password) {
      alert("Confirm Password should match the Password");
      return false;
    }

    
      
    
  




      // Store the mobile number for OTP verification
      window.userMobileForOtp = document.getElementById("phone").value;

      // Trigger OTP sending process (AJAX)
      sendOtp();

      // Prevent form submission here because it will be handled by sendOtp()
      return false;
    }

    function sendOtp() {
      // Perform AJAX request to send OTP
      var xhr = new XMLHttpRequest();
      xhr.open("POST", "/sendOtp", true);
      xhr.setRequestHeader("Content-Type", "application/json");
      xhr.onreadystatechange = function() {
        if (xhr.readyState === XMLHttpRequest.DONE) {
          // Handle response from server if needed
          // For example, you can redirect the user to OTP verification page after successful OTP sending
          if (xhr.status === 200) {
            window.location.href = "/otpVerification";
          } else {
            alert("OTP sending failed");
          }
        }
      };

      // Send data to the server (in this case, we are just sending the mobile number)
      var data = JSON.stringify({ number: window.userMobileForOtp });
      xhr.send(data);
    }
  </script>
</body>
</html>












<!DOCTYPE html>
<html>

<head>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
  <style>
    /* Your CSS styling here... */
  </style>
</head>

<body>
  <form method="post" action="/otpsignup" onsubmit="return handleOtpSignup()">
    <h1>Sign Up</h1>
    <div class="input-group">
      <i class="fas fa-user"></i>
      <input type="text" name="name" placeholder="Username" id="name" required>
    </div>
    <div class="input-group">
      <i class="fas fa-envelope"></i>
      <input type="email" name="email" placeholder="Email" id="email" required>
    </div>
    <div class="input-group">
      <i class="fas fa-phone"></i>
      <input class="message" type="number" name="mobile" placeholder="Mobile" id="phone" required>
    </div>
    <div class="input-group">
      <i class="fas fa-lock"></i>
      <input type="password" name="password" placeholder="Password" id="password" required>
    </div>
    <div class="input-group">
      <i class="fas fa-lock"></i>
      <input type="password" name="confirm_password" placeholder="Confirm Password" id="confirm_password" required>
    </div>
    <button type="submit" id="signupBtn">Sign up</button>
    <p>
      <span>Already have an account?</span>
      <a href="/login">Sign in here</a>
    </p>
  </form>

  <form method="post" action="/otpverification" id="otpForm" style="display: none;">
    <h1>OTP Verification</h1>
    <div class="input-group">
      <i class="fas fa-mobile"></i>
      <input type="number" name="otp" placeholder="Enter OTP" id="otpInput" required>
    </div>
    <button type="submit" id="otpVerifyBtn">Verify OTP</button>
    <p>
      <span id="otpErrorMessage" style="color: red;"></span>
    </p>
  </form>

  <script>
    function handleOtpSignup() {
      // Validate form fields...

      // Store the mobile number for OTP verification
      window.userMobileForOtp = document.getElementById("phone").value;

      // Trigger OTP sending process (AJAX)
      sendOtp();

      // Prevent form submission here because it will be handled by sendOtp()
      return false;
    }

    function sendOtp() {
      // Perform AJAX request to send OTP
      var xhr = new XMLHttpRequest();
      xhr.open("POST", "/otpsignup", true);
      xhr.setRequestHeader("Content-Type", "application/json");
      xhr.onreadystatechange = function () {
        if (xhr.readyState === XMLHttpRequest.DONE) {
          // Handle response from server if needed
          // For example, you can show OTP verification form after successful OTP sending
          if (xhr.status === 200) {
            document.getElementById("signupBtn").style.display = "none";
            document.getElementById("otpForm").style.display = "block";
          } else {
            alert("OTP sending failed");
          }
        }
      };

      // Send data to the server (in this case, we are just sending the mobile number)
      var data = JSON.stringify({ number: window.userMobileForOtp });
      xhr.send(data);
    }
  </script>
</body>

</html>
